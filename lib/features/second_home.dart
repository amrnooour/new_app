import 'package:flutter/material.dart';
import 'package:new_app/features/second_home_body.dart';

class SecondHome extends StatelessWidget {
  const SecondHome({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: SecondHomeBody(),
    );
  }
}